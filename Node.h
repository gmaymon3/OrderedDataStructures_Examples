#pragma once
class Node
{
    public:
        int data;         // To store the data
        Node* next;       // Pointer to the next node in the list

        // Constructor declaration
        Node(int value);
};

